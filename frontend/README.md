# Frontend

Static dashboard designed to run locally on this laptop.

Set the backend URL in `config.js`:

```js
window.TWIQ_API_BASE_URL = "http://127.0.0.1:8000";
```

For local development:

```bash
python3 -m http.server 4174
```
